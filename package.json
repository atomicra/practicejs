{
  "name": "express",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "SET NODE_ENV=development&&nodemon ./index",
    "inspect": "SET NODE_ENV=development&&nodemon --inspect-brk ./index --ignore ./build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.18.1",
    "stats.js": "^0.17.0",
    "three": "^0.151.3",
    "ws": "^8.8.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
